services:
  app:
    image: ${DOCKER_USERNAME}/${DEPLOY_TYPE}:${DEPLOY_TAG}
    restart: always
    deploy:
      replicas: 3
    environment:
      - PORT=3000
  proxy:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app